spring:
  data:
    redis:
      host: ${REDIS_HOST}
      port: 18467
      username: default
      password: ${REDIS_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

superset:
  provider: db

datasources:
  primary:
    jdbc-url: ${PRIMARY_JDBC_URL}
    username: ${DB_USERNAME}
    password: ${PRIMARY_DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  secondary:
    jdbc-url: ${SECONDARY_JDBC_URL}
    username: ${DB_USERNAME}
    password: ${SECONDARY_DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

app:
  security:
    init-vector: W1D>EI2u8.[=[bDj
    secret-key: CAF/}0=v16A:Wp!8]l86Rx!2czeB_6=9
    jwt-secret-key: zT>7D4v9F5~qQ{d1tL|k\ta[yN^ZX3Byo+]%/4gBHfmHL0N-VR